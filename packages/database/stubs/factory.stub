import { Factory } from '@elyvel/database'
import { {{ model }} } from '../../app/Models/{{ model }}.ts'

export class {{ class }} extends Factory<{{ model }}> {
  readonly model = {{ model }}

  /**
   * The attributes a generated row starts with — fill these in.
   *
   * Empty on purpose. `make:model X -mf` writes a migration with `id` and
   * timestamps and nothing else, so any column named here would be a column the
   * table does not have: `table posts has no column named name`, from two files
   * one command wrote. Add a line here as you add the column there.
   *
   * `index` is 0-based and unique per generated row, so derive unique values
   * from it rather than from a random source, which collides with a unique
   * index about one run in fifty:
   *
   * ```ts
   * definition(index: number) {
   *   return {
   *     title: `Post ${index}`,
   *     slug: `post-${index}`
   *   }
   * }
   * ```
   */
  definition(index: number) {
    return {}
  }
}
