/**
 * The application's JavaScript.
 *
 * Most of a server-rendered application needs very little of this — the pages
 * arrive as HTML — so this starts empty on purpose rather than with a framework
 * you would have to remove. Import what you need; Vite builds it, hashes it, and
 * the manifest tells the layout which file to ask for.
 *
 * The stylesheet is a second entry point in `vite.config.ts` rather than an
 * import here, so a page can carry the CSS without waiting for the JavaScript.
 * Importing it from this file works too — `env.d.ts` declares the module — and
 * is the right move once the styles belong to a component.
 */
console.debug('%c⚡ Elvel', 'color: #7c3aed; font-weight: bold')
