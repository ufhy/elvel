{
  "name": "@elvel-tools/xss-scan",
  "private": true,
  "version": "0.0.0",
  "description": "The XSS scanner's toolchain, pinned to the TypeScript its CLI can run on. Run it from the repository root: bun run xss:scan",
  "devDependencies": {
    "@kitajs/html": "4.2.13",
    "@kitajs/ts-html-plugin": "^4.1.4",
    "typescript": "~5.9.3"
  }
}
